{% for facet in facets %}
  <div {{ facet.attributes.addClass('facet-block') }}>
    {% if show_title %}
      <h3 class="facet-block__title">{{ facet.title }}</h3>
    {% endif %}
    <div class="facet-block__content">{{ facet.content }}</div>
  </div>
{% endfor %}
